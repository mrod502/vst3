#pragma once
#include <juce_audio_processors/juce_audio_processors.h>

namespace adhd {
    const juce::String APPLICATION_VERSION = "${ADHD_PLUGIN_VERSION}";
    const juce::String APPLICATION_NAME = "${ADHD_PLUGIN_NAME}";
}
